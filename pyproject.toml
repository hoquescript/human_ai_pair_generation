[project]
name = "human-ai-pair-generation"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "accelerate>=1.12.0",
    "pandas>=3.0.2",
    "pillow>=12.0.0",
    "torch>=2.11.0",
    "transformers>=5.7.0",
]
